[project]
name = "tsukuru"
version = "0.2.0"
description = "vocabulary and concept randomizer for Japanese sentence construction review"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.uv]
exclude-newer = "1 week"

[tool.uv.sources]
kezuru = { workspace = true }

[tool.uv.workspace]
members = ["apps-uv/*"]
